{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "calcDb",
            "database": "${workspaceFolder:server}/calculatorDb.sqlite3"
        }
    ]
}